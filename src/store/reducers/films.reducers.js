const INIT_STATE = {
    films: null
}

const filmsReducer = ( state = INIT_STATE, action) => {
    switch (action.type) {
        default: {
            return INIT_STATE
        }
    }
}

export default filmsReducer;
